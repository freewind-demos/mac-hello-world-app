import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, world!")
            .padding()
            .frame(width: 200, height: 100)
    }
}

#Preview {
    ContentView()
} 